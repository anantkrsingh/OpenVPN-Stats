<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Execute OpenVPN Command</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            padding: 20px;
        }
        textarea {
            width: 100%;
            height: 200px;
            margin-top: 10px;
        }
    </style>
</head>
<body>
    <h1>Execute OpenVPN Command</h1>

    <form action="/openvpn/execute-command" method="POST">
        <label for="command">Enter OpenVPN Command:</label><br>
        <input type="text" id="command" name="command" required>
        <button type="submit">Send Command</button>
    </form>

    <% if (error) { %>
        <p style="color: red;"><%= error %></p>
    <% } %>

    <% if (response) { %>
        <h2>Response:</h2>
        <textarea readonly><%= response %></textarea>
    <% } %>
</body>
</html>
